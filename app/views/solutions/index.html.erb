Solution Index Page

<% @solutions.each do |solution| %>
  <p><%= render partial: 'solution', locals: {:solution => solution} %><p>
<% end %>

<%# @solutions = Solution.all.first %>
  <%# render partial: 'hosts/host', locals: {host: @solution.host} %>
