<div class="container" id="bump-down">
  <div class="row">
    <div class="col-lg-12">
      <legend>Housing Report</legend>
    </div>
    <div class="col-lg-5">

      <p>Out of <%= Host.all.count %> total hosts and <%= Guest.all.count %> total guests, you
      were able to match <%= Arrangement.hosts.uniq.count %> hosts and
      <%= Arrangement.guests.count %> guests.  Your hosts are
      at <%= (Arrangement.guests.count.to_f/Host.capacity.to_f)*100 %>% capacity with
      <%= Guest.all.count-Arrangement.guests.count %> unmatched guests.
      </p>
      <p>
      <%= link_to("Arrangements", create_arrangement_path, class:"btn btn-default btn-lg fa fa-lock network-name", :method => :post) %></p>

      <% Arrangement.hosts.uniq.each do |host| %>
        <p>Host name: <%= host.name %><br>
          <% host.guests.each do |guest| %>
          - Guest name: <%= guest.name %><br>
          <% end %></p>
      <% end %>

  </div>
</div>
